# Picotility

app_name = TactileWeb
author = NellowTCS
repo_url = https://github.com/NellowTCS/TactileWeb.git
# commit = abc123  # Optional: pin to specific commit
# branch = main    # Optional: branch to clone (default: main)
subdir = tactility-src
# build_script =   # Optional: custom build command
# output_dir = build  # Optional: build output directory (default: build)
# skip_build = false  # Optional: skip build step (default: false)
